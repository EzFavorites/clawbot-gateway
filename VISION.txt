# ClawBot Gateway — 版本统一管理

## 版本号规则

采用 **语义化版本** `MAJOR.MINOR.PATCH`：

- **MAJOR**: 不兼容的 API 变更
- **MINOR**: 向下兼容的功能新增
- **PATCH**: 向下兼容的问题修复

## 版本来源

VISION.txt 是 **唯一版本权威来源**。所有其他地方的版本号通过构建脚本自动注入。

## 当前版本

```
VERSION = 0.1.1
```

## 版本映射关系

| 位置 | 版本来源 | 说明 |
|------|---------|------|
| `VISION.txt` | 手动维护 | **唯一权威来源** |
| `internal/version/version.go` | `-ldflags` 注入 | 构建时由 `build-fpk.sh` 读取写入 |
| `web/package.json` | 手动同步 | 前端版本号，独立于构建版本 |
| `web/src-tauri/tauri.conf.json` | 手动同步 | Tauri 桌面端版本号，与 VISION.txt 一致 |
| `EzFavorites/FnDepot/clawbot-gateway/manifest` | 手动同步或 CI 设置 | FPK 版本号，发布于 FnDepot 仓库 |
| Git tag | `v${VERSION}` | 发布时打 tag |

## 发布流程

```bash
# 1. 更新 VISION.txt 中的 VERSION
# 2. 同步 web/package.json 版本
# 3. 提交并打 tag: git tag v0.0.3 && git push origin v0.0.3
# 4. CI 自动构建 FPK 和 Docker 镜像
```

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 0.0.1 | - | 初始版本 |
| 0.0.2 | - | 架构重构：iLink 服务端 ForwardFunc |
| 0.0.3 | 2026-07-31 | QR 码修复、登录跳转、图标修复、打包路径修复 |
| 0.0.4 | 2026-08-02 | 日志系统重构：文本格式、结构化日志、SSE 实时推送 |
| 0.0.5 | 2026-08-02 | 日志模块划分优化：消除 gin 依赖、修复 notify 日志接入 |
| 0.0.6 | 2026-08-02 | 日志审计修复：reply_chars、统一 msg 格式、补充缺失日志 |
| 0.0.7 | 2026-08-02 | CSP 修复：添加 img-src data: 允许内联二维码图片 |
| 0.1.1 | 2026-08-16 | 修复 CI release 重复 assets 问题，重新发布 |
